etanol = float(input('qual é o valor do etanol?'))
gasolina = float(input('qual é o valor da gasolina?'))
valor = etanol/gasolina
if valor <0.7:
    print (' no momento o etanol está mas economico ')
else:
    print ('no momento a gasolina está mas economica')
#ecercucui 2
t = int(input('quantos litros de combustivel cabem no tanque do seu veiculo?'))
g = float(input('roda usando gasolina'))
e = float(input('roda usando etanol'))
